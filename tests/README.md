# Tests

Tests are divided by responsibility.

Architecture tests protect authority boundaries, causal integrity, execution timing, data admission, strategy admission, and research-job identity.

A test file existing in the repository is not evidence that the test passed; CI or an equivalent verified runtime must establish that.