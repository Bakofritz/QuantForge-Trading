# Releases

This directory contains immutable release evidence.

Each stable release should include a manifest, validation record, user manual, and build artifact where repository size and GitHub limits permit.

Legacy artifacts are retained for provenance and are not part of the active source tree.