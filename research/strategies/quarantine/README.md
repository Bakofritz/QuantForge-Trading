# Strategy Quarantine

Imported strategies and indicators enter here conceptually before research admission.

Required sequence:

quarantine → fingerprint → static scrub → capability inventory → user-selected feature set → sanitized representation → regression validation → research admission.

Original source is preserved separately from the sanitized representation.